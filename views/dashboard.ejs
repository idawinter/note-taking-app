<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Express Auth Demo</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1>Dashboard</h1>
    <div class="dashboard-content">
      <% if (user) { %>
        <div class="user-profile">
          <% if (user.photo || profile?.avatar) { %>
            <img src="<%= user.photo || profile.avatar %>" alt="Profile Picture" class="profile-pic">
          <% } %>
          <h2>Welcome, <%= user.displayName %>!</h2>
          <% if (user.email) { %>
            <p>Email: <%= user.email %></p>
          <% } %>
          
          <% if (user.googleId) { %>
            <p>Google ID: <%= user.googleId %></p>
          <% } else if (user.facebookId) { %>
            <p>Facebook ID: <%= user.facebookId %></p>
          <% } %>
        </div>

        <% if (profile) { %>
          <div class="user-preferences">
            <h3>Your Preferences</h3>
            <ul>
              <li>Font Size (live): <%= profile.preferences.fontSize %>px</li>
              <li>Font Color: <%= profile.preferences.fontColor %></li>
              <li>Font Family: <%= profile.preferences.fontFamily %></li>
              <li>Note Background: <%= profile.preferences.noteBackground %></li>
            </ul>
            <% if (profile.avatar) { %>
              <img src="<%= profile.avatar %>" alt="Avatar" class="profile-pic">
            <% } %>
          </div>
        <% } %>

        <div class="auth-info">
          <h3>Authentication Success!</h3>
          <% if (user.googleId) { %>
            <p>You’ve successfully authenticated with <strong>Google</strong> OAuth.</p>
          <% } else if (user.facebookId) { %>
            <p>You’ve successfully authenticated with <strong>Facebook</strong> OAuth.</p>
          <% } else { %>
            <p>You are logged in via an unknown provider.</p>
          <% } %>
          <p>In a real application, you would now have access to protected resources.</p>
        </div>
        
        <a href="/logout" class="logout-btn">Logout</a>
      <% } else { %>
        <p>You need to login to view this page.</p>
        <a href="/" class="login-link">Go to Login</a>
      <% } %>
    </div>
  </div>
</body>
</html>
