<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Preferences</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1>Edit Your Preferences</h1>

    <form action="/dashboard/preferences" method="POST">
      <label for="fontSize">Font Size (in px):</label>
      <input type="number" name="fontSize" value="<%= profile.preferences.fontSize %>" required>

      <label for="fontColor">Font Color (hex):</label>
      <input type="text" name="fontColor" value="<%= profile.preferences.fontColor %>" required>

      <label for="fontFamily">Font Family:</label>
      <input type="text" name="fontFamily" value="<%= profile.preferences.fontFamily %>" required>

      <label for="noteBackground">Note Background Color (hex):</label>
      <input type="text" name="noteBackground" value="<%= profile.preferences.noteBackground %>" required>

      <button type="submit">Save Preferences</button>
    </form>

    <br>
    <a href="/dashboard">← Back to Dashboard</a>
  </div>
</body>
</html>
